[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    accounts: tests for the Accounts service
    transactions: tests for the Transactions service
    smoke: critical happy path tests
